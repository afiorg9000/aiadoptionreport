# Dependencies — installed locally / by Netlify on every deploy.
# Do not check in. (28k+ files; was previously tracked in error.)
node_modules/

# Generated on `vite` / `vite build` (see vite.config.ts)
public/source-library.json

# Vite build output — Netlify rebuilds on every deploy; do not check in.
dist/

# Stray nested clone (created during agent setup; keep out of repo)
aiadoptionreport/

# Editor / OS noise
.DS_Store
.vscode/
.idea/

# Logs and local env
*.log
.env
.env.local

# Raw member interview data — held confidentially by LLPA, not for repo distribution.
# Local copy preserved on disk; just untracked from git.
Interview Responses-Grid view (*).csv
